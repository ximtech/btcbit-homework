name: build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check Out Code 
      uses: actions/checkout@v4

    - name: Set Up JDK
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '21'
        cache: 'gradle'

    - name: Build And Run Tests
      run: gradle clean build --info --stacktrace
      
    - name: Upload JAR to file store
      uses: JantHsueh/upload-file-action@master
      with:
        url: https://file.io
        forms: '{"Authorization":"Bearer ${{ secrets.FILE_IO_API_KEY }}"}'
        fileForms: '{"file":"build/libs/task-1.0.0.jar"}'
        
    - name: Update JAR Download Badge
      uses: schneegans/dynamic-badges-action@v1.7.0
      with:
        auth: ${{ secrets.GIST_BADGE_SECRET }}
        gistID: 8f6459c2417de7534f64d98360dde867
        filename: test.json
        label: Hello
        message: World
        color: orange

    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v4.0.1
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        slug: ximtech/btcbit-homework